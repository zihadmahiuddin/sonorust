[package]
name = "sonorust-cranelift"
version = "0.1.0"
edition = "2024"

[dependencies]
cranelift = { version = "0.121.1", features = ["jit", "module", "native"] }
sonorust-ir = { workspace = true }
sonorust-runtime = { workspace = true }
